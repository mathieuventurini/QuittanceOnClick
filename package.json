{
    "name": "quittance-on-click-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd client && npm install && npm run build && cd .. && rm -rf dist && mv client/dist dist"
    },
    "engines": {
        "node": "18.x"
    }
}